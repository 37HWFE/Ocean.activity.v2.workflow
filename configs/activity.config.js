// 活动配置
module.exports = econfig = {
    eventName: 'clxyy' // 活动标识
    /*entryJsPath: 'ujoyexchange/js/pc.js',  // PC页JS主入口，默认不修改
    entryHtmlPath: 'ujoyexchange/index.html', // PC页模板主入口，默认不修改
    entryMJsPath: 'ujoyexchange/js/mobile.js', // Mobile页JS主入口，默认不修改
    entryMHtmlPath: 'ujoyexchange/indexm.html' // Mobile页模板主入口，默认不修改*/
}